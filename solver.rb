# take the user input
# split it into rows and columns - define rows and columns
# iterate over each row to see if numbers can go in them
# then do the same for columns, then boxes
# keep checking if the puzzle is solved
class Sudoku
  def initialize(array)
    @array = array
  end
end
